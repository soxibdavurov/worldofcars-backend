# VPS Caddy snippet for api.dunyo.express
# Path: /root/caddy-server/Caddyfile (merge with existing config)

api.dunyo.express {
	encode gzip zstd

	@uploads path /uploads/*
	header @uploads Cache-Control "public, max-age=31536000, immutable"

	reverse_proxy 127.0.0.1:3007
}
